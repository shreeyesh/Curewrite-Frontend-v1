import * as React from "react";
import { StyleSheet, View } from "react-native";

const CategoryButtonExt = () => {
  return <View />;
};

const styles = StyleSheet.create({});

export default CategoryButtonExt;
